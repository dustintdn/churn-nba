# Core data stack
numpy>=1.26
pandas>=2.0

# Modeling & evaluation
scikit-learn>=1.4   # preprocessing pipelines, CV, metrics, calibration
xgboost>=2.0        # primary gradient-boosted classifier

# Explainability
shap>=0.44          # global + local model explanations

# Visualization
matplotlib>=3.8
seaborn>=0.13

# Production serving
fastapi>=0.110      # /predict + /health serving endpoints
uvicorn>=0.29       # ASGI server to run the FastAPI app
pydantic>=2.6       # request/response validation schemas
joblib>=1.3         # model artifact serialization

# Notebook
jupyter>=1.0
notebook>=7.0

# Interactive consumer (dashboard)
streamlit>=1.30      # upload-a-CSV / score-one-customer UI over the model

# Testing
pytest>=8.0          # unit + integration tests
httpx>=0.27          # FastAPI TestClient transport
